import styled from "styled-components";

export const StyledInputPassword = styled.div`
  position: relative;
  display: inline-block;

  > svg {
    position: absolute;
    top: 16px;
    right: -50px;
    cursor: pointer;
  }
`